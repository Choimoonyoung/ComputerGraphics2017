PA#3

p에 따라 bunny를 택하고, 혹은 cow를 택하고 맞게 변환합니다.
v 모드에서는 viewing 좌표계를 따라 물체들이 움직이고,
m 모드에서는 modeling 좌표계를 따라 물체들이 움직입니다.
선언한 변수들을 보면 key를 켜고 끄는 것은 bool으로 선언하여 true 혹은 껐다면 false로 설정합니다.
축을 결정하거나, 평면을 결정하는 변수들은 대부분 int로 선언하여, 1일때는 평면, 2일때는 축 등으로 설정하였습니다.

cow2wld의 좌표계를 보면 
00 01 02 03
10 11 12 13
20 21 22 23
30 31 32 33
이런 식으로 구성이 되어 있는데, 이중에서도 00~22까지는 자신의 좌표축을 가지고 있습니다.
그래서 이 부분은 camera 좌표계로 바꾸어 주고 이동시킨 뒤, 다시 자신의 좌표계로 대체한다면 translate가 v모드에서 잘 됩니다.

또한 rotate를 할 때
좌표계 * (x,y,z)T = (x1,y1,z1)
인데, 여기서 (x1,y1,z1)에 해당하는 부분은 rotate를 할 때 기준으로 돌고 있는 축이 됩니다.
그래서 좌표계를 inverse하여 오른쪽에 곱해준다면 glRotatef 의 변수로 무엇을 넣어야 할 지 결정할 수 있게 됩니다.
angle같은 경우는 알아서 설정해줍니다.